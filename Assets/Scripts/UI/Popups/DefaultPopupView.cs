namespace UI
{
    public sealed class DefaultPopupView : PopupView<DefaultPopup>
    {
        new public void Setup(DefaultPopup settings)
        {
            base.Setup(settings);
        }
    }
}
